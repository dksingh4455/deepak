/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}, // Adds browser-specific prefixes for CSS
  },
};

export default config;

