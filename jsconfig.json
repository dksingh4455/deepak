{
  "compilerOptions": {
    "baseUrl": ".",  // Set the base directory to the root of the project
    "paths": {
      "@/*": ["src/*"]  // This tells the compiler to map '@' to 'src' directory
    }
  }
}
